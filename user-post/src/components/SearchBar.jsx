import React from 'react';
import { Search } from 'semantic-ui-react';
const SearchBar = () => {
  return (
    <React.Fragment>
      <Search />
    </React.Fragment>
  );
};

export default SearchBar;
